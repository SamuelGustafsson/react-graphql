# This is whats shaping your schema, this file is specifying what is going to be possible to reach from the client

type Dog {
  name: String!
}

type Mutation {
  createDog(name: String!): Dog
}

type Query {
  dogs: [Dog]!
}
